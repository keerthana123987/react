
export default function Profile({ college, branch }) {

    return (
        <div>
            <h1>
                <p>{college}</p>
                <p>{branch}</p>
        </h1>
    </div>

    );

}

Profile.defaultProps; {

}